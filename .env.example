# Random secret used to sign session JWTs. Generate with: openssl rand -hex 32
SESSION_SECRET=

# Provisioned automatically by Vercel Blob (Storage tab). Also doubles as
# the app's data store (users/generations JSON) — see src/lib/db.ts.
BLOB_READ_WRITE_TOKEN=
